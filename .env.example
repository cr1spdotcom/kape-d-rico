# GoHighLevel credentials — copy to .env for local dev, or set in Vercel.
# NEVER commit a real .env file.
GHL_TOKEN=your-private-integration-token
GHL_LOCATION_ID=your-location-id
GHL_LIST_ID=your-list-id
